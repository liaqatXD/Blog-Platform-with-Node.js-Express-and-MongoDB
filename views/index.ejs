<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head.ejs') %>

  
  <body>
    <div class="title">
      <h1>Blogs</h1>
    </div>
    <div class="create-blog"><a href="/blogs/create">Create New Blog</a></div>

    <% if(blogs.length>0){ %><%for (const blog of blogs ) { %>

    <div class="blog-link">
      <a href="/blogs/<%= blog._id %>">
        <h1><%= blog.title %></h1>
        <p><%= blog.snippet %></p>
      </a>
    </div>
    <% } } else {%>
      <div class="no-blog">
        <div>
          <h1>No blogs found😢</h1>
      </div>

      <% } %>


  </body>
</html>
